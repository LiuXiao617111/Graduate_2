@model IEnumerable<MvcGraduate.Models.Article>

<div class="share_art">
    @if (Model != null)
    {
        var i = 0;
        if ( i++ < 3)
        {
            foreach (var item in Model)
            {
                <div class="my_art_detailed">
                    <div class="line1">
                        [@Convert.ToDateTime(item.Time).ToString("d")]<span>@Html.DisplayFor(modelItem => item.Title)</span>
                    </div>
                    <div class="line2">@Html.DisplayFor(modelItem => item.Contents)</div>
                    <div class="line3">
                        <span>
                            <a href="javascript:void();" onclick=" window.open('myxxdt/wzgl_detail.php?id=48', '', 'height=500,width=820,status=1,toolbar=no,menubar=no,location=no,scrollbars=yes,left=200,top=100,resizable=yes'); " data-article-id="48">查看详细</a>|
                            <a href="javascript:void();">评论</a>|
                            <a href="javascript:void();">分享</a>
                        </span>
                    </div>
                </div>
            }
        }
    }
</div>